# Использовать настройки только в данной папке и всех вложенных
root = true

# Конфигурация для всех ([*]) файлов в текущей папке
[*]
charset = utf-8
indent_style = space
indent_size = 2
end_of_line = crlf
trim_trailing_whitespace = true
insert_final_newline = true
